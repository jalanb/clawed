# clawed

`clawed` helps provide contexts to `Claude`

## Affordances

`clawed` can, for each project
- keep histories of chats with Claude
- transcibe meeting and extract "TODOs" from them
- enforce a Pythonic "style"

## Architecture

`clawed` maintains a directory per project
`clawed`'s project directories and kept separate from the projects themselves
  - but can be linked in there as needed (e.g. Serena)

